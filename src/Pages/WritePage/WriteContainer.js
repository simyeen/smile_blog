import React from "react";
import styled from "styled-components";
import WritePresenter from "./WritePresenter";

const WriteContainer = () => {
  return <WritePresenter></WritePresenter>;
};

export default WriteContainer;
